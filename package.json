{
  "private": true,
  "scripts": {
    "lint": "prettier --check . && dotnet format --verify-no-changes --verbosity diagnostic",
    "lint:fix": "prettier --write . && dotnet format --verbosity diagnostic",
    "test": "dotnet test --verbosity normal --nologo",
    "start": "dotnet run",
    "build": "dotnet build"
  },
  "devDependencies": {
    "prettier": "^3.2.5"
  }
}
